import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
  pickerItemStyle: {
    borderBottomWidth: 1
  },

  pickerStyle: {
    width: '100%',
    color: '#FCFCFC'
  }
});

export default Styles;
